# Repository Cleanup - 2026-04-29

## Actions Taken

1.  **Directory Structure Cleanup:**
    *   Created `_SOLUTIONS/` directory.
    *   Created `_SCRIPTS/` directory.
    *   Moved `2026-04-27-codex-install-solution.md` to `_SOLUTIONS/`.
    *   Moved `create_issues.sh` to `_SCRIPTS/`.

2.  **Script Standardization:**
    *   Created `_SCRIPTS/create_issues_generic.sh` as a boilerplate for mass issue creation using the GitHub CLI.

3.  **Agent Context Standardization:**
    *   Updated `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md` to have identical content (verified via `diff`).
    *   Synchronized content across all three agent files, with only the titles delineated for the specific LLM model.
    *   Integrated the comprehensive list of subagents from the `subagents/` directory into the Repository Map and Agent Selection Guide.
    *   Added the **Subagent Delegation Protocol** to all agent files, including a limit of **20 subagents** per session.

4.  **Epilogue Template Updates:**
    *   Updated `templates/epilogue_claude.md`, `templates/epilogue_codex.md`, and `templates/epilogue_gemini.md` to include mandatory instructions for maintaining content parity across agent files (verified via `diff` or checksum).

## File Moves

| Original Path | New Path |
| :--- | :--- |
| `2026-04-27-codex-install-solution.md` | `_SOLUTIONS/2026-04-27-codex-install-solution.md` |
| `create_issues.sh` | `_SCRIPTS/create_issues.sh` |

## Verification
*   `diff AGENTS.md CLAUDE.md && diff AGENTS.md GEMINI.md` (or equivalent `sha256sum` comparison) confirms content parity across all three agent files (titles excepted).
*   The new `_SOLUTIONS/` and `_SCRIPTS/` directories were created, and the documented file moves place `2026-04-27-codex-install-solution.md` and `create_issues.sh` under those directories.
*   Subagent delegation limits are clearly documented in the root instructions.
